super + shift + Esc
	pkill -SIGUSR1 sxhkd
super + Return
	$TERMINAL
super + p
	dmenu_run
super + l
	slock
{XF86AudioRaiseVolume,XF86AudioLowerVolume}
	pulsemixer --change-volume {+,-}5 ; \
	kill -s RTMIN "$(cat ~/.cache/bar.pid)"
super + w
	$BROWSER
super + shift + w
	$PRIVATE_BROWSER
super + a
	arandr
